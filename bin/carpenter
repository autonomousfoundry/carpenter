#!/usr/bin/env ruby
require 'rubygems'
require "carpenter"


definitions = Carpenter::DefinitionCollection.new
definitions.load_specifications File.join(Dir.pwd, "requirements.json")
definitions.load_definitions File.join(Dir.pwd, "requirements/**/*.rb")

build = Carpenter::Build.new(definitions.specifications, definitions.requirements, definitions.plans)
build.run

